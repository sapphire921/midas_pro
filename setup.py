"""Setup for midas_pro"""

import setuptools

setuptools.setup(name='midas_pro',
                 version='0.1',
                 description='Python version of Mixed Data Sampling (MIDAS) regression (allow for multivariate MIDAS)',
                 url='https://github.com/sapphire921/midas_pro',
                 author='sapphire921',
                 install_requires=[],
                 author_email='',
				 license='MIT',
                 packages=setuptools.find_packages(),
                 zip_safe=False)